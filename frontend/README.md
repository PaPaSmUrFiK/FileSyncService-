
  # High-Fidelity Web App Mockup

  This is a code bundle for High-Fidelity Web App Mockup. The original project is available at https://www.figma.com/design/BajaVkfWxxj61ymoShWljV/High-Fidelity-Web-App-Mockup.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  